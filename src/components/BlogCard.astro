---
import type { CollectionEntry } from 'astro:content';
import { Image } from 'astro:assets';
import { formatDate } from '@utils/format';
import Tag from './Tag.astro';

interface Props {
  post: CollectionEntry<'blog'>;
}
const { post } = Astro.props;
---

<article class="card card-hover group relative flex h-full min-w-0 flex-col overflow-hidden p-6">
  {
    post.data.heroImage && (
      <Image
        src={post.data.heroImage}
        alt=""
        width={480}
        height={240}
        class="-mx-6 -mt-6 mb-5 h-40 w-[calc(100%+3rem)] object-cover"
      />
    )
  }
  <div class="flex items-center gap-3 text-xs text-fg-faint">
    <time datetime={post.data.pubDate.toISOString()}>{formatDate(post.data.pubDate)}</time>
    <span aria-hidden="true">·</span>
    <span>{post.data.category}</span>
  </div>
  <h3 class="mt-3 break-words text-lg font-semibold leading-snug tracking-tight text-fg">
    <a href={`/blog/${post.id}`} class="transition-colors after:absolute after:inset-0 group-hover:text-primary">
      {post.data.title}
    </a>
  </h3>
  <p class="mt-2 line-clamp-3 min-w-0 flex-1 break-words text-sm text-fg-muted">{post.data.description}</p>
  {
    post.data.tags.length > 0 && (
      <div class="mt-4 flex flex-wrap gap-2">
        {post.data.tags.slice(0, 3).map((tag) => (
          <Tag tag={tag} />
        ))}
      </div>
    )
  }
</article>
